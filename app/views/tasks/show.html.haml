%h1= @task.title
%p
  %strong
  = @task.description
.tags
  %strong Tags :
  = raw @task.task_tags

%br/
%br/
= link_to 'Editer la tâche', edit_task_path(@task), class: 'btn btn-primary'
=link_to 'Retour à la liste', tasks_path, class: 'btn btn-default pull-right'
